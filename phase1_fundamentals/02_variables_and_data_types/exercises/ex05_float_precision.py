"""
Exercise 5: Floating point precision
1. Print the result of 0.1 + 0.2. Notice it's not exactly 0.3.
2. Show that (0.1 + 0.2) == 0.3 evaluates to False.
3. Use math.isclose() to correctly compare (0.1 + 0.2) to 0.3.
"""

import math

# TODO: print 0.1 + 0.2

# TODO: print the direct == comparison (should be False)

# TODO: use math.isclose to compare correctly (should be True)
