"""
Exercise 3: Type conversion
Given the string inputs below (pretend they came from input()), convert
each to the correct type and perform the requested operation.
"""

price_str = "19.99"
quantity_str = "3"
in_stock_str = "0"

# TODO: convert price_str to float and quantity_str to int,
#       then compute and print the total cost (price * quantity)

# TODO: convert in_stock_str to bool using int() first, then bool()
#       print whether the item is in stock (True/False)
#       (think about why bool("0") alone would give the wrong answer)
